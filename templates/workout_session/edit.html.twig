{% extends 'base.html.twig' %}

{% block title %}Edit WorkoutSession{% endblock %}

{% block body %}
    <h1>Edit WorkoutSession</h1>

    {{ include('workout_session/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_workout_session_index') }}">back to list</a>

    {{ include('workout_session/_delete_form.html.twig') }}
{% endblock %}
