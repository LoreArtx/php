<form method="post" action="{{ path('app_workout_program_delete', {'id': workout_program.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ workout_program.id) }}">
    <button class="btn">Delete</button>
</form>
