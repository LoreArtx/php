{% extends 'base.html.twig' %}

{% block title %}WorkoutProgram{% endblock %}

{% block body %}
    <h1>WorkoutProgram</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ workout_program.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ workout_program.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ workout_program.description }}</td>
            </tr>
            <tr>
                <th>Duration</th>
                <td>{{ workout_program.duration }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_workout_program_index') }}">back to list</a>

    <a href="{{ path('app_workout_program_edit', {'id': workout_program.id}) }}">edit</a>

    {{ include('workout_program/_delete_form.html.twig') }}
{% endblock %}
